import core from './_core';

// Placeholder — real per-screen sections get merged in here once their
// translation files exist (see sibling subdirectories). Screens under
// active translation may show raw key names until that merge lands.
export default core;
